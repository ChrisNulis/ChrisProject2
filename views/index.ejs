<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>This is Index Page</h1>
    <ul>
      <% for(let i = 0; i < items.length; i++) { %>
        <li>
          <a href="/collections/<%=items[i].id%>">
            <%=items[i].name %>
          </a>
          <img src="<%=items[i].img%>" alt=<%=items.name%>>
          <form action="/collections/<%=items[i].id%>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
          </form>
          <a href="/collections/<%=items[i].id%>/edit">Edit</a>
        </li>
      <%}%>
    </ul>
    <nav>
      <a href="/collections/new">Create new Pop</a>
    </nav>










  </body>
</html>
